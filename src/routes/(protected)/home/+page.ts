import { get } from 'svelte/store';
import { isLogin } from '../../../store/test.js';
import { redirect } from '@sveltejs/kit';

/** @type {import('./$types').PageLoad} */
export function load({ params }) {



}